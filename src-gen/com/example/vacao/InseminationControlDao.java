package com.example.vacao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.example.vacao.InseminationControl;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table InseminationControl.
*/
public class InseminationControlDao extends AbstractDao<InseminationControl, Long> {

    public static final String TABLENAME = "InseminationControl";

    /**
     * Properties of entity InseminationControl.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Comment = new Property(1, String.class, "comment", false, "COMMENT");
        public final static Property Date = new Property(2, java.util.Date.class, "date", false, "DATE");
        public final static Property CattleId = new Property(3, long.class, "cattleId", false, "CATTLE_ID");
        public final static Property BullOriginId = new Property(4, long.class, "bullOriginId", false, "BULL_ORIGIN_ID");
    };

    private DaoSession daoSession;

    private Query<InseminationControl> cattle_InseminationControlQuery;
    private Query<InseminationControl> bullOrigin_InseminationControlListQuery;

    public InseminationControlDao(DaoConfig config) {
        super(config);
    }
    
    public InseminationControlDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'InseminationControl' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'COMMENT' TEXT," + // 1: comment
                "'DATE' INTEGER," + // 2: date
                "'CATTLE_ID' INTEGER NOT NULL ," + // 3: cattleId
                "'BULL_ORIGIN_ID' INTEGER NOT NULL );"); // 4: bullOriginId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'InseminationControl'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, InseminationControl entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String comment = entity.getComment();
        if (comment != null) {
            stmt.bindString(2, comment);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(3, date.getTime());
        }
        stmt.bindLong(4, entity.getCattleId());
        stmt.bindLong(5, entity.getBullOriginId());
    }

    @Override
    protected void attachEntity(InseminationControl entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public InseminationControl readEntity(Cursor cursor, int offset) {
        InseminationControl entity = new InseminationControl( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // comment
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // date
            cursor.getLong(offset + 3), // cattleId
            cursor.getLong(offset + 4) // bullOriginId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, InseminationControl entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setComment(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDate(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setCattleId(cursor.getLong(offset + 3));
        entity.setBullOriginId(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(InseminationControl entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(InseminationControl entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "InseminationControl" to-many relationship of Cattle. */
    public List<InseminationControl> _queryCattle_InseminationControl(long cattleId) {
        synchronized (this) {
            if (cattle_InseminationControlQuery == null) {
                QueryBuilder<InseminationControl> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.CattleId.eq(null));
                cattle_InseminationControlQuery = queryBuilder.build();
            }
        }
        Query<InseminationControl> query = cattle_InseminationControlQuery.forCurrentThread();
        query.setParameter(0, cattleId);
        return query.list();
    }

    /** Internal query to resolve the "inseminationControlList" to-many relationship of BullOrigin. */
    public List<InseminationControl> _queryBullOrigin_InseminationControlList(long bullOriginId) {
        synchronized (this) {
            if (bullOrigin_InseminationControlListQuery == null) {
                QueryBuilder<InseminationControl> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.BullOriginId.eq(null));
                bullOrigin_InseminationControlListQuery = queryBuilder.build();
            }
        }
        Query<InseminationControl> query = bullOrigin_InseminationControlListQuery.forCurrentThread();
        query.setParameter(0, bullOriginId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getCattleDao().getAllColumns());
            builder.append(" FROM InseminationControl T");
            builder.append(" LEFT JOIN CATTLE T0 ON T.'CATTLE_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected InseminationControl loadCurrentDeep(Cursor cursor, boolean lock) {
        InseminationControl entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Cattle cattle = loadCurrentOther(daoSession.getCattleDao(), cursor, offset);
         if(cattle != null) {
            entity.setCattle(cattle);
        }

        return entity;    
    }

    public InseminationControl loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<InseminationControl> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<InseminationControl> list = new ArrayList<InseminationControl>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<InseminationControl> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<InseminationControl> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
